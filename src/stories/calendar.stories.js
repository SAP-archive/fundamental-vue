import storiesFor from '../util/stories-for'

const stories = storiesFor('calendar')

export default { title: 'calendar' }

export const bindings = stories('bindings')
export const blockedItems = stories('blocked-items')
export const buttons = stories('buttons')
export const customNames = stories('custom-names')
export const defaultDate = stories('default-date')
export const defaultCalendar = stories('default')
export const disabledItems = stories('disabled-items')
export const firstDayOfWeek = stories('first-day-of-week')
export const grid = stories('grid')
export const noHeader = stories('no-header')
export const popover = stories('popover')
export const selectionRange = stories('selection-range')
