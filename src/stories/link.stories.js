import storiesFor from '../util/stories-for'

const stories = storiesFor('link')

export default { title: 'Link' }

export const defaultLink = stories('default')
export const VueRouter = stories('vue-router-links')
