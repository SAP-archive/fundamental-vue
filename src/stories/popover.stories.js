import storiesFor from '../util/stories-for'

const stories = storiesFor('popover')

export default { title: 'popover' }

export const defaultPopover = stories('default-popover')
export const customContent = stories('custom-content')
export const customControls = stories('custom-controls')
export const styles = stories('default-styles')
export const placementOption = stories('placement-options')
