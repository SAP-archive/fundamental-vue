import storiesFor from '../util/stories-for'

const stories = storiesFor('menu-popover')

export default { title: 'menu-popover' }

export const defaultMenuPopover = stories('default-menu-popover')
export const placementOptions = stories('menu-popover-placement')
