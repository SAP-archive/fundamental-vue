import storiesFor from '../util/stories-for'

const stories = storiesFor('shell-bar')

export default { title: 'shell-bar' }

export const actions = stories('actions')
export const minimal = stories('minimal')
export const products = stories('products')
