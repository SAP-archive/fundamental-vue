import storiesFor from '../util/stories-for'

const stories = storiesFor('action-bar')

export default { title: 'Action Bar' }

export const backButtonSlot = stories('back-button-slot')
export const everything = stories('everything')
export const headerTitleSlot = stories('header-title-slot')
