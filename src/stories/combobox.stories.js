import storiesFor from '../util/stories-for'

const stories = storiesFor('combobox')

export default { title: 'combobox' }

export const defaultCombobox = stories('default')
export const customMenu = stories('custom-menu')
export const sizes = stories('sizes')
