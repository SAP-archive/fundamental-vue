import storiesFor from '../util/stories-for'

const stories = storiesFor('time-picker')

export default { title: 'time-picker' }

export const defaultTimePIcker = stories('default')
export const multipleTimes = stories('multiple-times')
export const time = stories('time')
