import storiesFor from '../util/stories-for'

const stories = storiesFor('section')

export default { title: 'section' }

export const header = stories('header')
export const panels = stories('panels')
export const structure = stories('structure')
