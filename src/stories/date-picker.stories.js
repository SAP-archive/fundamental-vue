import storiesFor from '../util/stories-for'

const stories = storiesFor('date-picker')

export default { title: 'Date Picker' }

export const defaultDatePicker = stories('date-picker-default')
export const customDateePicker = stories('date-picker-custom')
