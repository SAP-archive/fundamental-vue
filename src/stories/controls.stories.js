import storiesFor from '../util/stories-for'

const stories = storiesFor('controls')

export default { title: 'Controls' }

export const checkboxDefault = stories('checkbox-default')
export const compactTextArea = stories('text-area-compact')
export const defaultTextArea = stories('text-area-default')
export const textAreaWithStates = stories('text-area-states')
export const compactTextInput = stories('text-input-compact')
export const defaultTextInput = stories('text-input-default')
export const textInputWithStates = stories('text-input-states')
export const toggle = stories('toggle')
