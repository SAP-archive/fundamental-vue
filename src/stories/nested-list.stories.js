import storiesFor from '../util/stories-for'

const stories = storiesFor('nested-list')

export default { title: 'Nested List' }

export const defaultNestedList = stories('default')
export const compactWithIcons = stories('compact-with-icons')
export const icons = stories('icons')
export const nested = stories('nested')
