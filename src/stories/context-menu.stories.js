import storiesFor from '../util/stories-for'

const stories = storiesFor('context-menu')

export default { title: 'context-menu' }

export const defaultContextMenu = stories('context-menu-default')
export const contextMenuCustomButton = stories('context-menu-custom-button')
