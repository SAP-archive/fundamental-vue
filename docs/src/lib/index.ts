export const iconClass = (icon: IconName): string => `sap-icon--${icon}`;
// tslint:disable-next-line:no-empty-interface
export type IconName = string; // rule disabled because we want a type name for this.
