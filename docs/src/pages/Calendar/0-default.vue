<title>Calendar</title>
<docs>The `FdCalendar` component is usually displayed inside an `FdPopover`. Scroll down to see an example if you are unsure how to do that. Even though the calendar is a very complex component getting started is as easy as pie. This
```
<FdCalendar />
```
gets you the default calendar displayed below. `FdCalendar` has a lot of customization points. Please refer to the examples below for details.
</docs>
<tip>`FdCalendar` is still under heavy development. Things might break.</tip>
<template>
  <div>
    <FdCalendar />
  </div>
</template>
